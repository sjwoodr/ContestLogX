{
  "name": "web",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=contestlogx-web --branch=main",
    "deploy:preview": "npm run build && wrangler pages deploy dist --project-name=contestlogx-web"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.18",
    "astro": "^5.16.15",
    "tailwindcss": "^4.1.18"
  },
  "devDependencies": {
    "wrangler": "^3.94.0"
  }
}
